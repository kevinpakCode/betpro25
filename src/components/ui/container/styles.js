import { createUseStyles } from 'react-jss'
const userStyles = createUseStyles({
  container: {
    position: 'relative',
    width: '100%',
    maxWidth: '1230px',
    padding: '0 15px',
    margin: '0 auto'
  }
  
})

export default userStyles